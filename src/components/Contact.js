import React from 'react'

const Contact = (props) => {
  // console.log('contact', props)
  // setTimeout(() => {
  //   props.history.push('/about')
  // }, 2000);
  return (
    <div>
      Contact
    </div>
  )
}

export default Contact
